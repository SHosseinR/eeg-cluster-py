I have eeg dataset. two groups. healthy and patient. I want you to build full code for this analysis. 
eeg data should be read. data is  preprocessed so no preprocessing needed. 
chunk data to 10 seconds epochs. for each epoch, EEG signals should be bandpass-filtered into five canonical frequency bands—delta (1–4 Hz), theta (4–8 Hz), alpha (8–13 Hz), beta (13–30 Hz), and gamma (30–45 Hz). Then for each, functional connectivity matrix should be extracted. 
the goal is directed weighted graph. usePartial Directed Coherence (PDC), Spectral Granger Causality (GC), Phase Slope Index (PSI). and do PLV too (not directed but for visualization. i will explain later.) 
All 4 methods, above with pre-existing functions (like in mne or anything else). do not implement matrix extraction method, yourself. (if you did not find, leave the function prototype. although i think you will find.)
Connectivity should be computed on the filtered time series for each frequency band and epoch. 
then matrices on all epochs are averaged (per subject). so you have a matric for each frequency band, method and subject. each matrix (per method, per band, per subject.) should be normalized. 
visualization 1: The connectivity matrices get averaged across all frequency bands for visualization. plot each connectivity extraction method matrix. so if 4 methods, plot 4 matrices. (averaged across subjects). (these averaging are just for this plot. not further analysis.) the color of matrix visulization represents node strength value. 
visualization 2: Calculate the p-value for each node in matrix. (across subjects. you have normalized graphs. n_band*n_subject for each method. so for each band and method, you have n_subject matrices. you ploted averaged across subjects before. not again this but add p-value to each node. the p-value calculation is on hypothesis of s the average being un-equal to zero.) this time, the color represents p-value. 
    
visualization 3: do same but this time per band (Averaged over methods).
now choose one method (i will change it later based on visualization 2 result.). analysis still per band. (visualization 3 is only for visualization :)). 
this new part. compute network measures. compute these metrices. (use already existing libraries like bctpy or whatever.)
Global efficiency
local efficiency 
Clustering coefficient 
transitivity
modularity
Degree
betweenness centrality
Rich-club coefficient
assortativity
Spectral radius
and more if you think is good and i forget. 
give one measure per graph. so n_band * n_subject set of metrics. 
each measure, is one per graph. (nodal measures should be averaged i think.)
use n_subject dimension for p-value calculation. as i discuss later on, i have 2 unpaired groups of subjects. patiend and healthy. test is on the non-equality of these two groups. (all calculations above are done on both. so you have to have all data of two subject groups.)
visualization 4: a tabel of measure * band. each cell, the p-value. colored by the p-value. 
next part. i want to test separability of two groups by each measures. on the number of Combination of 3 measures, i have a set of metrics. for example for 10 measures, there is 120 combination of 3 of them. i wanna find the best set which separates the two classes. use logistic regression for classification. 
Select one triplet. 5 fold cv. train fold 80 percent. test 5 times (cross validation scenario). report average accuracy. repeate of 120 triplet. take 10 best sets.
visualization 5: table of top 10 sets of best feature triplet with accuracy.
visualization 6: for the best feature set, plot barplot . each of 3 measures with the importance. (logistic regression weights.) 

## 
I have implemented all above. 
D:.
│   classification.py
│   config.py
│   connectivity.py
│   custom_connectivity_plot.png
│   data_loader.py
│   main.py
│   network_measures.py
│   PROJECT_OVERVIEW.md
│   README.md
│   requirements.txt
│   setup.py
│   signal_processing.py
│   statistics_utils.py
│   TROUBLESHOOTING.md
│   utils.py
│   visualization.py
├───results
│   ├───data
│   ├───figures
│   └───reports
└───test_data

this is the main file (up to connectivity calculation):
"""
Main pipeline for EEG connectivity analysis
"""

import os
import numpy as np
import pandas as pd
from datetime import datetime

# Import all modules
from config import (
    HC_DATA_PATH, PATIENT_DATA_PATH, OUTPUT_DIR,
    FREQUENCY_BANDS, CONNECTIVITY_METHODS, SELECTED_METHOD,
    NETWORK_MEASURES, STEP_TO_START
)
from data_loader import load_group_data, verify_data_consistency
from signal_processing import process_subject_epochs
from connectivity import compute_all_connectivity
from network_measures import compute_network_measures_for_subjects, compute_all_network_measures
from statistics_utils import (
    compute_pvalue_matrix, compute_group_comparison_pvalues,
    extract_features_for_classification
)
from visualization import (
    plot_connectivity_matrices, plot_pvalue_matrices,
    plot_pvalue_matrices_per_band, plot_network_measures_pvalues,
    plot_top_feature_sets_per_band, plot_feature_importance_per_band,
    create_summary_report
)
from classification import (
    find_best_feature_triplets, get_best_triplet_details,
    create_classification_report
)

def create_output_directories():
    """Create output directories if they don't exist."""
    dirs = [
        OUTPUT_DIR,
        os.path.join(OUTPUT_DIR, 'figures'),
        os.path.join(OUTPUT_DIR, 'data'),
        os.path.join(OUTPUT_DIR, 'reports')
    ]
    for dir_path in dirs:
        os.makedirs(dir_path, exist_ok=True)
    print(f"Output directories created in: {OUTPUT_DIR}")


def main():
    """Main analysis pipeline."""
    
    print("\n" + "="*80)
    print("EEG CONNECTIVITY ANALYSIS PIPELINE")
    print("="*80)
    
    # Create output directories
    create_output_directories()
    
    # ========================================================================
    # STEP 1: LOAD DATA
    # ========================================================================
    print("\n" + "="*80)
    print("STEP 1: LOADING DATA")
    print("="*80)

    if STEP_TO_START <= 1:
        healthy_data = load_group_data(HC_DATA_PATH, group_name="Healthy")
        patient_data = load_group_data(PATIENT_DATA_PATH, group_name="Patient")
        
        print(f"\nLoaded:")
        print(f"  Healthy: {len(healthy_data)} subjects")
        print(f"  Patient: {len(patient_data)} subjects")
        
        # Verify consistency
        all_data = healthy_data + patient_data
        if not verify_data_consistency(all_data):
            raise ValueError("Data consistency check failed!")
        
    # ========================================================================
    # STEP 2: SIGNAL PROCESSING (EPOCHING & FILTERING)
    # ========================================================================
    print("\n" + "="*80)
    print("STEP 2: SIGNAL PROCESSING")
    print("="*80)

    if STEP_TO_START <= 2:
        all_subjects_filtered = {}
        
        for group_data, group_name in [(healthy_data, "Healthy"), (patient_data, "Patient")]:
            all_subjects_filtered[group_name] = {}
            
            for subject in group_data:
                subject_id = subject['subject_id']
                print(f"\nProcessing {subject_id} ({group_name})...")
                
                filtered_epochs = process_subject_epochs(subject['data'], subject['fs'])
                all_subjects_filtered[group_name][subject_id] = {
                    'filtered_epochs': filtered_epochs,
                    'fs': subject['fs'],
                    'channels': subject['channels']
                }
        
    # ========================================================================
    # STEP 3: CONNECTIVITY ANALYSIS
    # ========================================================================
    print("\n" + "="*80)
    print("STEP 3: CONNECTIVITY ANALYSIS")
    print("="*80)

    if STEP_TO_START <= 3:
        connectivity_matrices = {}
        
        for group_name, subjects_dict in all_subjects_filtered.items():
            connectivity_matrices[group_name] = {}
            
            for subject_id, subject_data in subjects_dict.items():
                print(f"\nComputing connectivity for {subject_id} ({group_name})...")
                
                conn_results = compute_all_connectivity(
                    subject_data['filtered_epochs'],
                    subject_data['fs'],
                    methods=CONNECTIVITY_METHODS
                )
                
                connectivity_matrices[group_name][subject_id] = conn_results
        
        # Save connectivity matrices
        np.save(os.path.join(OUTPUT_DIR, 'data', 'connectivity_matrices.npy'), 
                connectivity_matrices, allow_pickle=True)
        print(f"\nSaved connectivity matrices")
        
##
so you can load it.
for example:
connectivity_matrices = np.load(
    os.path.join(OUTPUT_DIR, 'data', 'connectivity_matrices.npy'),
    allow_pickle=True
).item()
connectivity_matrices.get('Healthy').get('H S2 EC').get('plv').get('theta').shape
(19, 19)
it's an adjacency matrix.


Now I want these below: 
All below is only on one connectivity calculation method (e.g. plv, psi, gc, ...) (take it from SELECTED_METHOD in config.py).

Now I want to build an optimization app. 
Using the NSGA-II algorithm, you should search for optimal combinations of node and frequency that simultaneously minimizes/maximize these criteria:
3 of these network measures.
measure_functions = {
    'global_efficiency': compute_global_efficiency,
    'local_efficiency': compute_local_efficiency,
    'clustering_coefficient': compute_clustering_coefficient,
    'transitivity': compute_transitivity,
    'modularity': compute_modularity,
    'degree': compute_degree,
    'betweenness_centrality': compute_betweenness_centrality,
    'rich_club': compute_rich_club,
    'assortativity': compute_assortativity,
    'spectral_radius': compute_spectral_radius,
    'small_worldness': compute_small_worldness,
    'diameter': compute_diameter
} 
example usage:
measure = measure_functions['global_efficiency'](adjacency_matrix)
#
all these functions are in network_measures.py file. you can import. 
the matter is, only 3 of these. and take the names of these tree from a list variable in config.py (create variable name yourself).
the optimization runs independently for each Patient subject (only on patient subjects. not Healthy).
the criteria (maximize or minimizes that measure) is decided by the difference between average of measures of all Patients vs all Healthy. each patient and each healthy has measures. this step has been done before too (but for all measures not just 3):
##
print("\n" + "="*80)
print("STEP 5: COMPUTING NETWORK MEASURES")
print("="*80)
print(f"Using connectivity method: {SELECTED_METHOD.upper()}")

if STEP_TO_START <= 5:
    # Filter connectivity matrices to selected method only
    selected_connectivity = {}
    for group_name in connectivity_matrices.keys():
        selected_connectivity[group_name] = {}
        for subject_id, methods_dict in connectivity_matrices[group_name].items():
            if SELECTED_METHOD in methods_dict:
                selected_connectivity[group_name][subject_id] = {
                    SELECTED_METHOD: methods_dict[SELECTED_METHOD]
                }
    
    # Compute network measures
    network_measures = compute_network_measures_for_subjects(
        selected_connectivity,
        list(FREQUENCY_BANDS.keys())
    )
    
    # Save network measures
    np.save(os.path.join(OUTPUT_DIR, 'data', 'network_measures.npy'),
            network_measures, allow_pickle=True)
    print(f"\nSaved network measures")

## 
and you can load it:
network_measures = np.load(
    os.path.join(OUTPUT_DIR, 'data', 'network_measures.npy'),
    allow_pickle=True
).item()
example"
network_measures.get('Healthy').get('H S2 EC').get('gc').get('theta').get('global_efficiency') : shows a number e.g. 0.20

##

back to what you should do.
so you take average of selected 3 measures for all Healthy and for all Patient separatly, so two numbers for each measure. if Patient average for that metric is higher that Healthy, the optimization goal is to minimize the measure. and maximize for the reverse. the goal is to make Patient get closer to Healthy.

#
back to optimization variables. 
Using the NSGA-II algorithm, you should search for optimal combinations of node and frequency that optimize. 
By optimal combinations of node and frequency, I mean this. 

In each run (single run among lots of runs of the genetic algorithm), you choose node (of graph) and frequency. so for one subject (obviously genetic algorithm runs independently for each subject), there is one adjacency matrix here. 
here now stimulation happens to drive Patient to Healthy. It's state space dynamics. 
Xdot = A*X - A*Xbar + B*U, X0 = Xbar ; Xbar is average activation of signals that one Patient. you have to load it from .set files. 
filtered_epochs = process_subject_epochs(subject['data'], subject['fs'])
def process_subject_epochs(data, fs):
    """
    Complete epoch processing pipeline for a single subject.
    
    Parameters
    ----------
    data : ndarray, shape (n_channels, n_samples)
        Continuous EEG data
    fs : float
        Sampling frequency
        
    Returns
    -------
    filtered_epochs : dict
        Dictionary mapping band names to filtered epochs
        Each value has shape (n_epochs, n_channels, n_samples)
    """
(i put this for you to know the input output. here we don't need epochs. just average over all time is enough) 
#
back to equation: Xdot = A*X - A*Xbar + B*U, X0 = Xbar 
-A*Xbar exists to that with zero input, no change happens. 
and here U and B. based on the node selected (in genetic algorithm), apply U value 1, time 1 second (take from config.py), and that's it. (you A is obviously the adjacency matrix of the frequency band selected by algorithm.) (normalize A in order to have maximum eigenvalue less than 0. so the dynamic won't be unstable. you can do this like this:
w, _ = eig(A)
l = np.abs(w).max()
A_norm = A / (c + l)
A_norm = A_norm - np.eye(A.shape[0])
)
the simulation is easy too. like this ( i copied from somewhere else:)

    # state vectors to float if they're bools
    if type(x0[0]) == np.bool_:
        x0 = x0.astype(float)

    # check dimensions of states
    if x0.ndim == 1:
        x0 = x0.reshape(-1, 1)

    # Simulate trajectory
    T = np.size(U, 1)
    N = np.size(A_norm, 0)

    # initialize x
    x = np.zeros((N, T))
    xt = x0

    for t in range(T):
        x[:, t] = xt[:, 0]
        dt = np.matmul(A_norm, xt) + np.matmul(B, np.reshape(U[:, t], (N, 1)))  # state equation
        dt = dt * 0.001
        xt = dt + xt
# but this is for standard state space. -A*Xbar does not exist. same. you can change code as you want.  

then new state X is activation of nodes after stimulation. now derive a array of size n_nodes. each element represents how much that node, increased or decreased after stimulation. (the ratio. 1 for no change.)

now is the neat part. we need to apply changes to adjacency matrix. (like plasticity in brain :)).
each edge in the matrix (suppose E(n,m)) gets multiplied by h = (R n)*(R m). (R n) is ratio of increase in node n and so as (R m) (node m). 
now normalize connectivity (with the normalize_connectivity_matrix function in connectivity.py) and re-compute the 3 measures. done! now you can calculate the 3 objectives of genetic algorithm. 
# hint of the function schema:
def normalize_connectivity_matrix(matrix):
    """
    Normalize connectivity matrix to [0, 1] range.
    
    Parameters
    ----------
    matrix : ndarray, shape (n_channels, n_channels)
        Connectivity matrix
        
    Returns
    -------
    normalized : ndarray
        Normalized connectivity matrix
    """
##

run the NSGA algorithm for each Patient subject. at last each subject would have a result of node and frequency parameter. save the results. give a plot too. histogram of nodes (with channel name legend), histogram of frequency, and 2d histogram of node x frequency with count value as color heat map of each cell. 

#
now give me the full implementation of above. Modular code. you can give multiple files for modular code.





## codex
this project. the connectivity extraction part and the nsga II optimization part. can you do it in multi process way? the code here, only use maximum one core cpu. 
i am talking about the two parts. 
there is one part extracting connectivity matrices (which takes a while to run) in the main.py: step 3. compute_all_connectivity function. .
and the part to run nsga II optimization. (which also takes a while to run). in the run_optimization.py, optimizer.optimize_all_patients function. 
I think both are independently done per subject. So they can be run in parallel.